{
    "web": {
        "host": "localhost",
        "webroot": "/app",
        "SSL": {
            "enable": true,
            "port": 443,
            "certFile": "/certs/cert.pem",
            "keyFile": "/certs/privkey.pem"
        }
    }
}