routerUtil = (function() {
    generatedObject = {
        buildUrl: function(action) {
            var relativePath = './/?action=' + action;
            return relativePath;
        }
    };

    return generatedObject;
})();
